import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  searchContainer: {
    paddingVertical: 12,
  },
  image: {
    width: '100%',
    height: 300,
    opacity: 0.3,
    marginTop: 180,
  },
});
